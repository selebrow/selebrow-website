# UI

Selebrow comes with a simple UI which allows you to monitor running sessions and reset or connect via VNC to selected session
(if it was enabled for the session). The UI is enabled by default (unless running in CI environment) and accessible via the `http://localhost:4444/ui` URL.
